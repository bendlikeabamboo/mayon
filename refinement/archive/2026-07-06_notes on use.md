Couple of notes we should fix:

# Chat Automatically Scrolling to Bottom after Finishing.
While generating, we display the streaming text. But once it finishes the behavior right now is it jumps to the bottom once the whole text finishes. This is not desired behavior. We must be loyal to where the user's screen is at at the moment.

# Remove Jump to Bottom button.
It's very distracting and breaks the flow.

# AI Model on composer
Let's put it above the composer and on a smaller font.

# Search not working
Search is not working. I tried searching for "Azure" but no results, forever "Searching..."
`   