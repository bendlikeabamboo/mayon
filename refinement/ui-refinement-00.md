1. I want the line on on expound string to use the --highlight color and not make it dashed. When I click on it a context menu will pop up containing the expound chat that it is linked to.
2. For external links, I want it to be in --muted-foreground and with an external link icon (a small one) next to it.
3. For mermaid diagrams, is there a way we can have it previewed using a dynamic markdown previewer (like it has zoom, pan, etc.) in a window?
4. Can you make a plan to create the Labs, Quizzes, Siblings, Parents or whatever in the sidebar in the right? instead of at the bottom?
5. Support mathematics rendering.