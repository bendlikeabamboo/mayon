as developer i wish there's a bogus endpoint or provider which i can onboard onto so that we can have more automated tests using playwright. kinda like a mock llm provider. it will give us a static reply, regardless of whatever we're doing but i'd like the reply to be meaningful and will test a lot of stuff for us. so maybe something like a reaply with all elements that we support so we can test it.