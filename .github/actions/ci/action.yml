name: CI
description: Install deps, lint, type-check, test, build SPA, and verify Docker build
runs:
  using: composite
  steps:
    - shell: bash
      run: pnpm install --frozen-lockfile
    - shell: bash
      run: pnpm lint
    - shell: bash
      run: pnpm check
    - shell: bash
      run: pnpm test
    - shell: bash
      run: pnpm build
    - shell: bash
      run: docker build .
