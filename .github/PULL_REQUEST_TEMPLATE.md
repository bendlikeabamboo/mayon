## Summary

_A brief summary of what this PR does._

## Motivation

_Why is this change needed? What problem does it solve? Link relevant issues if applicable._

## Changes

_A bulleted list of the main changes introduced by this PR._

-

## Testing

- [ ] `pnpm lint` passes
- [ ] `pnpm check` passes (no type errors)
- [ ] `pnpm test` passes (all tests green)
- [ ] `pnpm build` succeeds

## Migration Note

If this PR changes `src/lib/db/schema.ts`:

- [ ] Ran `pnpm db:generate` and `pnpm bundle:migrations`
- [ ] Committed the updated migration files
